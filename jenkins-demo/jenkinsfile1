pipeline {
	agent any
  
  environment {
	MY_LOG = 'This is my log level'
	RELEASE = '0.4'

 }
	stages {
		stage('Build') {
		agent any
		
	steps {
	echo "Buildingg realease ${RELEASE} with log level ${MY_LOG}"

}
}
	stage('Test') {
	steps {
	echo "Testing. I can see my log ${MY_LOG}, but not release ${RELEASE}"
}
}
}
}
